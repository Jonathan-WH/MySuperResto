import { TotalCartPricePipe } from './total-cart-price.pipe';

describe('TotalCartPricePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalCartPricePipe();
    expect(pipe).toBeTruthy();
  });
});
