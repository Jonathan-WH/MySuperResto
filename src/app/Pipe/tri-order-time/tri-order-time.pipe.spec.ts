import { TriOrderTimePipe } from './tri-order-time.pipe';

describe('TriOrderTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TriOrderTimePipe();
    expect(pipe).toBeTruthy();
  });
});
